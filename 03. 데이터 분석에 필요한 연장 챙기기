#3.1. 변하는 수, '변수' 이해하기

#변수 만들기
a =1

#여러 값으로 구성된 변수 만들기
var1 = [1,2,3]

#문자로 된 변수 만들기
str1 = 'a'


#3.2. 마술 상자 같은 '함수' 이해하기

#함수 이용하기
x = [1,2,3]
print(sum(x)) #max()

#3.3. 함수 꾸러미, '패키지' 이해하기

#패키지 활용하기
import seaborn as sns
import matplotlib.pyplot as plt

# 데이터 생성
var = ['a', 'b', 'c', 'a']

# countplot 그리기
sns.countplot(x=var)
plt.show()
